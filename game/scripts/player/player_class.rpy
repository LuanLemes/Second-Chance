init python:
    class Player(object):
        def __init__(self, name, age, money):
            self.name = name
            self.age = age
            self.money = money

    player = Player("Almir", 0, 100)
